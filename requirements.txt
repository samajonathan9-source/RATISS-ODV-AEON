# RATISS Aeon Prime — Dépendances Python minimales (frugal, CPU-only)
# Core serveur
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
websockets>=12.0
# Requis par FastAPI pour les routes UploadFile/Form (import universel /api/files/upload)
python-multipart>=0.0.9

# Noyau scientifique
numpy>=1.24,<3.0
scipy>=1.11,<2.0
psutil>=5.9,<7.0

# Génération de contenu (PDF, graphiques)
matplotlib>=3.7,<4.0
fpdf2>=2.7,<3.0
# Extraction de texte PDF (requis par les tests PDF enrichis v9.4)
pypdf>=4.0.0

# Browser automation (type Manus IA)
playwright>=1.40,<2.0

# Recherche web (type Manus IA) — Tavily si clé, DuckDuckGo fallback sinon
tavily-python>=0.5,<1.0
requests>=2.28,<3.0

# Optionnel : topologie persistante (homologie). Si absent, fallback natif.
# gudhi est lourd (~50 Mo wheel) — décommenter si besoin de GUDHI réel.
# gudhi>=3.9

# Optionnel : biologie structurale (PDB parsing). Fallback si absent.
# biopython>=1.83
# rdkit>=2024.3

# Optionnel : connecteurs QPU. Fallback local si absent.
# qiskit>=1.0
# qiskit-ibm-runtime>=0.20
# perceval-quandela>=0.10

# Sécurité (déjà dans la stdlib pour PBKDF2, SQLite)
# bcrypt>=4.0  # optionnel pour hachage alternatif
# Vault de cles API (chiffrement au repos Fernet)
cryptography>=42.0,<46.0
